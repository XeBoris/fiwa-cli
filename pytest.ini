[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
pythonpath = src
addopts =
    -v
    --strict-markers
    --tb=short
    --asyncio-mode=auto

[pytest-watch]
clear = True
runner = textual run --dev main.py